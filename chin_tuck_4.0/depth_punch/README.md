# depth_punch — planned

Not built yet. Will be the punch-frame counterpart to
[`../depth_guard/`](../depth_guard/): the same click (chin tip + the
shoulder's most frontal point), but sampled from frames that fall *inside*
a punch instead of the non-punch band the guard variants restrict to.
